{{#paper-button onClick=(action 'locateNearest') raised=true class='btn-block' disabled=isSearching}}
  {{#if isSearching}}
    Searching {{paper-icon 'rotate-right' spin=true}}
  {{else}}
    Find Location
  {{/if}}
{{/paper-button}}
<br>

{{yield}}
